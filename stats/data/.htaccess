# Interdit tout accès HTTP direct à ce dossier (base SQLite, journaux WAL).
# Apache 2.4
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
# Apache 2.2 (repli)
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
